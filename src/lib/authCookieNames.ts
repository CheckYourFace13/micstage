/** Venue/artist session cookie; must match `src/lib/session.ts` usage. */
export const OM_SESSION_COOKIE_NAME = "om_session";

/** Short-lived claim-invite exchange session (HttpOnly). */
export const OM_CLAIM_INVITE_SESSION_COOKIE_NAME = "om_claim_invite";
